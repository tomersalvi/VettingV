import React, { useState, useEffect } from 'react';
import { 
  Shield, 
  BarChart3, 
  Search, 
  FileText, 
  AlertTriangle, 
  CheckCircle2, 
  TrendingUp, 
  PieChart, 
  Users, 
  ArrowRight,
  Menu,
  X,
  Lock,
  Zap,
  Activity
} from 'lucide-react';

// --- Animation Hooks ---
const useIntersectionObserver = (options = {}) => {
  const [elements, setElements] = useState([]);
  const [entries, setEntries] = useState([]);

  useEffect(() => {
    const observer = new IntersectionObserver((observedEntries) => {
      setEntries(observedEntries);
    }, options);
    elements.forEach((element) => observer.observe(element));
    return () => observer.disconnect();
  }, [elements, options]);

  return [setElements, entries];
};

const AnimatedSection = ({ children, className = "" }) => {
  const [isVisible, setIsVisible] = useState(false);
  const [ref, setRef] = useState(null);

  useEffect(() => {
    if (ref) {
      const observer = new IntersectionObserver(
        ([entry]) => {
          if (entry.isIntersecting) {
            setIsVisible(true);
            observer.disconnect();
          }
        },
        { threshold: 0.1 }
      );
      observer.observe(ref);
      return () => observer.disconnect();
    }
  }, [ref]);

  return (
    <div
      ref={setRef}
      className={`transition-all duration-1000 transform ${
        isVisible ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-10'
      } ${className}`}
    >
      {children}
    </div>
  );
};

// --- Components ---

const Navbar = ({ scrollToSection }) => {
  const [isOpen, setIsOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 20);
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  return (
    <nav className={`fixed w-full z-50 transition-all duration-300 border-b ${
      scrolled 
        ? 'bg-[#0B0F19]/80 backdrop-blur-xl border-white/10 py-3' 
        : 'bg-transparent border-transparent py-5'
    }`}>
      <div className="container mx-auto px-6 flex justify-between items-center">
        {/* Logo Section */}
        <div className="flex items-center space-x-3 group cursor-pointer">
          <div className="bg-gradient-to-tr from-blue-600 to-indigo-600 p-2 rounded-lg shadow-lg shadow-blue-500/20 group-hover:shadow-blue-500/40 transition-all">
             {/* Vyzer-style simplified logo icon */}
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M12 2L2 7L12 12L22 7L12 2Z" stroke="white" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/>
              <path d="M2 17L12 22L22 17" stroke="white" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/>
              <path d="M2 12L12 17L22 12" stroke="white" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/>
            </svg>
          </div>
          <div className="flex flex-col">
            <span className="text-[10px] uppercase tracking-widest text-slate-400 font-semibold leading-none mb-0.5">Powered by</span>
            <span className="text-xl font-bold text-white leading-none tracking-tight">vyzer.co</span>
          </div>
        </div>

        {/* Desktop Menu */}
        <div className="hidden md:flex items-center space-x-8 text-sm font-medium">
          <button onClick={() => scrollToSection('features')} className="text-slate-300 hover:text-white transition-colors">Features</button>
          <button onClick={() => scrollToSection('how-it-works')} className="text-slate-300 hover:text-white transition-colors">How it Works</button>
          <button onClick={() => scrollToSection('pricing')} className="text-slate-300 hover:text-white transition-colors">Pricing</button>
          <button 
            onClick={() => scrollToSection('signup')}
            className="bg-white text-slate-900 hover:bg-slate-200 px-5 py-2.5 rounded-full font-bold transition-all transform hover:scale-105"
          >
            Request Access
          </button>
        </div>

        {/* Mobile Menu Button */}
        <button className="md:hidden text-white" onClick={() => setIsOpen(!isOpen)}>
          {isOpen ? <X /> : <Menu />}
        </button>
      </div>

      {/* Mobile Menu */}
      {isOpen && (
        <div className="md:hidden absolute top-full left-0 w-full bg-[#0B0F19] border-t border-white/10 p-6 flex flex-col space-y-4 shadow-2xl">
          <button onClick={() => { scrollToSection('features'); setIsOpen(false); }} className="text-slate-300 hover:text-white text-left py-2">Features</button>
          <button onClick={() => { scrollToSection('how-it-works'); setIsOpen(false); }} className="text-slate-300 hover:text-white text-left py-2">How it Works</button>
          <button onClick={() => { scrollToSection('pricing'); setIsOpen(false); }} className="text-slate-300 hover:text-white text-left py-2">Pricing</button>
          <button onClick={() => { scrollToSection('signup'); setIsOpen(false); }} className="bg-blue-600 text-white py-3 rounded-lg text-center font-bold">Get Started</button>
        </div>
      )}
    </nav>
  );
};

const FeatureCard = ({ icon: Icon, title, description, badge }) => (
  <div className="bg-white/5 border border-white/5 backdrop-blur-sm p-6 rounded-2xl hover:bg-white/10 hover:border-blue-500/30 transition-all duration-300 group">
    <div className="flex justify-between items-start mb-4">
      <div className="p-3 rounded-xl bg-blue-500/10 group-hover:bg-blue-500/20 transition-colors">
        <Icon className="w-6 h-6 text-blue-400" />
      </div>
      {badge && (
        <span className="text-[10px] font-bold uppercase tracking-wider bg-indigo-500/20 text-indigo-300 px-2 py-1 rounded-md border border-indigo-500/20">
          {badge}
        </span>
      )}
    </div>
    <h3 className="text-lg font-bold text-white mb-2">{title}</h3>
    <p className="text-slate-400 text-sm leading-relaxed">{description}</p>
  </div>
);

const AnalysisPreview = () => (
  <div className="relative mx-auto max-w-5xl mt-12 perspective-1000">
    {/* Glass Container */}
    <div className="relative bg-[#131B2C]/90 border border-white/10 rounded-2xl shadow-2xl overflow-hidden backdrop-blur-xl ring-1 ring-white/5">
      
      {/* Dashboard Header */}
      <div className="flex items-center justify-between px-6 py-4 border-b border-white/5 bg-[#0F1522]">
        <div className="flex items-center space-x-4">
          <div className="flex space-x-1.5">
            <div className="w-2.5 h-2.5 rounded-full bg-red-500/50"></div>
            <div className="w-2.5 h-2.5 rounded-full bg-yellow-500/50"></div>
            <div className="w-2.5 h-2.5 rounded-full bg-green-500/50"></div>
          </div>
          <div className="h-4 w-[1px] bg-white/10"></div>
          <span className="text-xs font-medium text-slate-400">Deal_Analysis_Orbit_Series_B.pdf</span>
        </div>
        <div className="flex items-center space-x-3">
            <span className="text-xs text-green-400 flex items-center bg-green-900/20 px-2 py-1 rounded">
                <Activity className="w-3 h-3 mr-1" /> Live Analysis
            </span>
        </div>
      </div>

      {/* Dashboard Body */}
      <div className="p-6 md:p-8 grid md:grid-cols-12 gap-6">
        
        {/* Left Column: Score */}
        <div className="md:col-span-4 space-y-6 border-r border-white/5 pr-6">
          <div>
            <h4 className="text-slate-500 text-xs font-bold uppercase tracking-widest mb-3">Vyzer Confidence Score</h4>
            <div className="relative flex items-center justify-center w-48 h-48 mx-auto">
              <svg className="transform -rotate-90 w-full h-full">
                <circle cx="96" cy="96" r="88" stroke="currentColor" strokeWidth="12" fill="transparent" className="text-slate-800" />
                <circle cx="96" cy="96" r="88" stroke="currentColor" strokeWidth="12" fill="transparent" strokeDasharray={552} strokeDashoffset={552 - (552 * 0.82)} className="text-blue-500" />
              </svg>
              <div className="absolute flex flex-col items-center">
                <span className="text-5xl font-bold text-white">82</span>
                <span className="text-sm text-slate-400 mt-1">out of 100</span>
              </div>
            </div>
          </div>
          <div className="bg-blue-500/10 rounded-lg p-4 border border-blue-500/20">
            <p className="text-blue-200 text-xs leading-relaxed">
              <strong>Verdict:</strong> Strong technical team and market fit. Valuation is slightly high for current revenue multiples compared to Q3 vintage.
            </p>
          </div>
        </div>

        {/* Right Column: Details */}
        <div className="md:col-span-8 space-y-6 pl-2">
            
            {/* Metrics Grid */}
            <div className="grid grid-cols-3 gap-4">
                <div className="bg-[#0B0F19] p-4 rounded-xl border border-white/5">
                    <div className="text-xs text-slate-500 mb-1">Team Exp.</div>
                    <div className="text-lg font-bold text-white">Top 5%</div>
                    <div className="w-full bg-slate-800 h-1 mt-2 rounded-full overflow-hidden">
                        <div className="bg-green-500 w-[95%] h-full"></div>
                    </div>
                </div>
                <div className="bg-[#0B0F19] p-4 rounded-xl border border-white/5">
                    <div className="text-xs text-slate-500 mb-1">Market Size</div>
                    <div className="text-lg font-bold text-white">$12.4B</div>
                    <div className="w-full bg-slate-800 h-1 mt-2 rounded-full overflow-hidden">
                        <div className="bg-blue-500 w-[70%] h-full"></div>
                    </div>
                </div>
                <div className="bg-[#0B0F19] p-4 rounded-xl border border-white/5">
                    <div className="text-xs text-slate-500 mb-1">Legal Risk</div>
                    <div className="text-lg font-bold text-yellow-400">Med</div>
                    <div className="w-full bg-slate-800 h-1 mt-2 rounded-full overflow-hidden">
                        <div className="bg-yellow-500 w-[40%] h-full"></div>
                    </div>
                </div>
            </div>

            {/* Red Flags List */}
            <div>
                <h4 className="text-slate-400 text-xs font-bold uppercase tracking-widest mb-3">Detected Red Flags</h4>
                <div className="space-y-3">
                    <div className="flex items-start space-x-3 bg-red-500/5 p-3 rounded-lg border border-red-500/10">
                        <AlertTriangle className="w-4 h-4 text-red-500 mt-0.5 shrink-0" />
                        <div>
                            <span className="text-red-200 text-sm font-semibold">Abnormal Fee Structure</span>
                            <p className="text-slate-400 text-xs mt-0.5">Management fees (2.5%) are 40bps higher than vintage average.</p>
                        </div>
                    </div>
                    <div className="flex items-start space-x-3 bg-yellow-500/5 p-3 rounded-lg border border-yellow-500/10">
                        <AlertTriangle className="w-4 h-4 text-yellow-500 mt-0.5 shrink-0" />
                        <div>
                            <span className="text-yellow-200 text-sm font-semibold">Runway Concern</span>
                            <p className="text-slate-400 text-xs mt-0.5">Current burn rate implies 8 months runway; deck claims 18 months.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
      </div>
    </div>
  </div>
);

const SignUpForm = () => {
  const [email, setEmail] = useState('');
  const [status, setStatus] = useState('idle');

  const handleSubmit = (e) => {
    e.preventDefault();
    setStatus('loading');
    setTimeout(() => {
      setStatus('success');
      setEmail('');
    }, 1500);
  };

  if (status === 'success') {
    return (
      <div className="bg-gradient-to-br from-green-900/40 to-slate-900 border border-green-500/30 p-8 rounded-2xl text-center backdrop-blur-md">
        <div className="bg-green-500/20 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4 ring-1 ring-green-500/50">
          <CheckCircle2 className="w-8 h-8 text-green-400" />
        </div>
        <h3 className="text-2xl font-bold text-white mb-2">Request Received</h3>
        <p className="text-slate-300 mb-6">You've been added to the Vyzer Vetting priority queue. We will be in touch shortly.</p>
        <button onClick={() => setStatus('idle')} className="text-blue-400 hover:text-blue-300 font-medium text-sm">
          Reset Form
        </button>
      </div>
    );
  }

  return (
    <div className="bg-[#131B2C] border border-white/5 p-8 md:p-10 rounded-2xl shadow-2xl relative overflow-hidden">
      {/* Decorative Glow */}
      <div className="absolute top-0 right-0 w-64 h-64 bg-blue-500/10 rounded-full blur-[80px] -z-0 pointer-events-none"></div>

      <div className="relative z-10">
        <h3 className="text-2xl font-bold text-white mb-2">Join the Vetting Engine Beta</h3>
        <p className="text-slate-400 mb-8 text-sm">Professionalize your due diligence workflow. Designed for Family Offices and Institutional LPs.</p>
        
        <form onSubmit={handleSubmit} className="space-y-5">
          <div>
            <label className="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">Work Email</label>
            <input 
              type="email" 
              required
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              className="w-full bg-[#0B0F19] text-white px-4 py-3.5 rounded-lg border border-slate-700 focus:border-blue-500 focus:ring-1 focus:ring-blue-500 outline-none transition-all placeholder-slate-600"
              placeholder="name@fund.com"
            />
          </div>
          <div>
            <label className="block text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">Organization Type</label>
            <div className="relative">
              <select className="w-full bg-[#0B0F19] text-white px-4 py-3.5 rounded-lg border border-slate-700 focus:border-blue-500 outline-none appearance-none">
                <option>Family Office</option>
                <option>Institutional LP</option>
                <option>VC Fund</option>
                <option>Angel Syndicate</option>
              </select>
              <div className="absolute right-4 top-1/2 -translate-y-1/2 pointer-events-none">
                 <svg className="w-4 h-4 text-slate-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M19 9l-7 7-7-7"></path></svg>
              </div>
            </div>
          </div>
          <button 
            type="submit" 
            disabled={status === 'loading'}
            className={`w-full bg-blue-600 hover:bg-blue-500 text-white font-bold py-4 rounded-lg transition-all transform hover:-translate-y-0.5 shadow-lg shadow-blue-600/20 flex items-center justify-center space-x-2 ${status === 'loading' ? 'opacity-75 cursor-wait' : ''}`}
          >
            {status === 'loading' ? (
              <span>Processing...</span>
            ) : (
              <>
                <span>Request Access</span>
                <ArrowRight className="w-4 h-4" />
              </>
            )}
          </button>
        </form>
      </div>
    </div>
  );
};

export default function App() {
  const scrollToSection = (id) => {
    const element = document.getElementById(id);
    if (element) {
      element.scrollIntoView({ behavior: 'smooth' });
    }
  };

  return (
    <div className="min-h-screen bg-[#0B0F19] text-slate-200 selection:bg-blue-500/30 selection:text-blue-200 font-sans overflow-x-hidden">
      <Navbar scrollToSection={scrollToSection} />

      {/* Hero Section */}
      <section className="relative pt-32 pb-20 md:pt-48 md:pb-32">
        {/* Background Gradients */}
        <div className="absolute top-0 left-1/2 -translate-x-1/2 w-full h-[800px] bg-gradient-radial from-blue-900/20 via-[#0B0F19]/0 to-transparent -z-10 pointer-events-none"></div>
        <div className="absolute top-20 right-0 w-[600px] h-[600px] bg-indigo-600/10 rounded-full blur-[120px] -z-10"></div>
        <div className="absolute bottom-0 left-0 w-[500px] h-[500px] bg-blue-600/5 rounded-full blur-[100px] -z-10"></div>

        <div className="container mx-auto px-6 text-center">
          <AnimatedSection>
            <div className="inline-flex items-center space-x-2 bg-blue-500/10 border border-blue-500/20 rounded-full px-4 py-1.5 mb-8 backdrop-blur-md">
              <span className="flex h-1.5 w-1.5 rounded-full bg-blue-400 shadow-[0_0_10px_rgba(96,165,250,0.8)]"></span>
              <span className="text-xs font-semibold text-blue-200 uppercase tracking-wide">Beta Access Open for LPs</span>
            </div>
            
            <h1 className="text-5xl md:text-7xl font-extrabold text-white tracking-tight mb-8 leading-tight">
              Vet Deals with <br className="hidden md:block" />
              <span className="bg-clip-text text-transparent bg-gradient-to-r from-blue-400 via-indigo-400 to-purple-400">Institutional Precision</span>
            </h1>
            
            <p className="text-lg md:text-xl text-slate-400 max-w-2xl mx-auto mb-12 leading-relaxed font-light">
              The Vyzer Vetting Engine empowers you to instantly analyze pitch decks, detect hidden risks, and benchmark opportunities against thousands of historical data points.
            </p>
            
            <div className="flex flex-col md:flex-row items-center justify-center space-y-4 md:space-y-0 md:space-x-4 mb-20">
              <button 
                onClick={() => scrollToSection('signup')}
                className="w-full md:w-auto bg-blue-600 hover:bg-blue-500 text-white px-8 py-4 rounded-full font-bold transition-all shadow-[0_0_30px_rgba(37,99,235,0.3)] hover:shadow-[0_0_40px_rgba(37,99,235,0.5)] flex items-center justify-center"
              >
                Start Vetting Deals
              </button>
              <button 
                onClick={() => scrollToSection('how-it-works')}
                className="w-full md:w-auto bg-[#131B2C] hover:bg-[#1E293B] text-white border border-slate-700 hover:border-slate-600 px-8 py-4 rounded-full font-bold transition-all flex items-center justify-center"
              >
                View Sample Report
              </button>
            </div>

            <AnalysisPreview />
          </AnimatedSection>
        </div>
      </section>

      {/* Trusted By - Social Proof */}
      <section className="py-12 border-y border-white/5 bg-[#0D121F]">
        <div className="container mx-auto px-6">
          <p className="text-center text-slate-500 text-xs font-bold uppercase tracking-[0.2em] mb-10">Trusted by Forward-Thinking Investment Teams</p>
          <div className="flex flex-wrap justify-center gap-10 md:gap-20 opacity-40 grayscale hover:grayscale-0 hover:opacity-100 transition-all duration-500">
             {/* Text-based Logos for demo */}
            {['VENTURE ONE', 'CAPITAL NORTH', 'BLUE HORIZON', 'APEX FUNDS'].map((brand, i) => (
              <div key={i} className="text-xl font-bold font-mono tracking-tighter text-white">
                 {brand}
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Features Grid */}
      <section id="features" className="py-24 relative">
        <div className="container mx-auto px-6 relative z-10">
          <div className="text-center mb-16">
            <h2 className="text-3xl md:text-5xl font-bold text-white mb-6">Due Diligence. <span className="text-blue-500">Automated.</span></h2>
            <p className="text-slate-400 text-lg max-w-2xl mx-auto">Stop reading 40-page PDFs manually. Let Vyzer extract, analyze, and benchmark the data that actually matters.</p>
          </div>

          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
            <FeatureCard 
              icon={BarChart3}
              title="Deck Analysis & Extraction"
              description="Instantly convert PDF pitch decks into structured JSON. We extract financials, KPIs, and projections automatically using OCR and LLMs."
              badge="Core"
            />
            <FeatureCard 
              icon={AlertTriangle}
              title="Risk & Red Flag Detection"
              description="Our engine flags inconsistencies, unrealistic growth projections, and omitted information that are standard in compliant deals."
            />
            <FeatureCard 
              icon={TrendingUp}
              title="Market Benchmarking"
              description="Compare the deal's valuation, burn rate, and CAC against a proprietary database of 10,000+ similar vintage deals."
              badge="New"
            />
            <FeatureCard 
              icon={Users}
              title="Founder Background Checks"
              description="Deep background checks on founding teams, aggregating data from past exits, failed ventures, and public sentiment."
            />
            <FeatureCard 
              icon={FileText}
              title="Fee Structure Analysis"
              description="Analyze GP fees, carry, and hurdle rates. We highlight when terms deviate from standard market norms."
            />
            <FeatureCard 
              icon={PieChart}
              title="Portfolio Impact"
              description="Visualize how this specific deal impacts your current portfolio's sector allocation, risk profile, and liquidity forecast."
            />
          </div>
        </div>
      </section>

      {/* How it works */}
      <section id="how-it-works" className="py-24 bg-[#0D121F] border-t border-white/5">
        <div className="container mx-auto px-6">
          <div className="grid md:grid-cols-2 gap-16 items-center">
            <div>
              <div className="inline-block px-3 py-1 rounded bg-blue-500/10 text-blue-400 text-xs font-bold uppercase tracking-wider mb-4">Workflow</div>
              <h2 className="text-3xl md:text-4xl font-bold text-white mb-8">From PDF to Investment Memo in Seconds</h2>
              <div className="space-y-12">
                {[
                  { title: 'Upload Deal Docs', desc: 'Drag and drop pitch decks, memos, or P&Ls. Bank-level encryption ensures total privacy.', icon: '01' },
                  { title: 'AI Extraction Engine', desc: 'Our models parse unstructured data, cross-referencing Crunchbase and Pitchbook APIs.', icon: '02' },
                  { title: 'Decision Ready Report', desc: 'Receive a standardized 10-page report highlighting risks, ops, and opportunities.', icon: '03' }
                ].map((step, i) => (
                  <div key={i} className="flex space-x-6 group">
                    <div className="flex-shrink-0 w-12 h-12 rounded-xl bg-white/5 border border-white/10 group-hover:border-blue-500/50 group-hover:bg-blue-500/10 transition-all flex items-center justify-center font-mono font-bold text-slate-500 group-hover:text-blue-400 text-lg">
                      {step.icon}
                    </div>
                    <div>
                      <h4 className="text-xl font-bold text-white mb-2 group-hover:text-blue-400 transition-colors">{step.title}</h4>
                      <p className="text-slate-400 leading-relaxed">{step.desc}</p>
                    </div>
                  </div>
                ))}
              </div>
            </div>
            
            {/* Visual Representation of Workflow */}
            <div className="relative">
              <div className="absolute inset-0 bg-gradient-to-tr from-blue-600/20 to-purple-600/20 blur-3xl rounded-full"></div>
              <div className="relative bg-[#131B2C] border border-white/10 rounded-2xl p-8 shadow-2xl">
                {/* File Drop Zone Visualization */}
                <div className="border-2 border-dashed border-slate-700 rounded-xl p-8 text-center mb-6 bg-[#0B0F19]">
                    <div className="mx-auto w-12 h-12 bg-slate-800 rounded-full flex items-center justify-center mb-3">
                        <FileText className="text-slate-400" />
                    </div>
                    <p className="text-sm text-slate-400">Processing <span className="text-white font-mono">Series_A_Deck.pdf</span></p>
                    <div className="w-full bg-slate-800 h-1.5 mt-4 rounded-full overflow-hidden">
                        <div className="bg-blue-500 w-2/3 h-full animate-pulse"></div>
                    </div>
                </div>

                {/* Processing Steps */}
                <div className="space-y-3">
                    <div className="flex items-center justify-between text-sm">
                        <span className="text-slate-400 flex items-center"><Zap className="w-3 h-3 mr-2 text-yellow-500"/> Extracting Financials</span>
                        <span className="text-green-400">Done</span>
                    </div>
                    <div className="flex items-center justify-between text-sm">
                        <span className="text-slate-400 flex items-center"><Search className="w-3 h-3 mr-2 text-blue-500"/> Vetting Founders</span>
                        <span className="text-green-400">Done</span>
                    </div>
                    <div className="flex items-center justify-between text-sm">
                        <span className="text-slate-400 flex items-center"><Shield className="w-3 h-3 mr-2 text-purple-500"/> Legal Compliance Check</span>
                        <span className="text-blue-400 animate-pulse">Running...</span>
                    </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section id="signup" className="py-24 relative overflow-hidden">
        <div className="container mx-auto px-6 relative z-10">
          <div className="grid md:grid-cols-2 gap-16 items-center">
            <div>
              <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">Ready to professionalize your deal flow?</h2>
              <p className="text-xl text-slate-400 mb-8 font-light">
                Join the platform built for sophisticated LPs who demand data-driven clarity in a noisy market.
              </p>
              <ul className="space-y-4 mb-10">
                {[
                  'Unlimited deck uploads during beta',
                  'Institutional-grade risk modeling',
                  'Export reports to PDF or Notion',
                  'Bank-level AES-256 encryption'
                ].map((item, i) => (
                  <li key={i} className="flex items-center space-x-3 text-slate-300">
                    <div className="bg-green-500/20 p-1 rounded-full">
                        <CheckCircle2 className="w-4 h-4 text-green-400" />
                    </div>
                    <span>{item}</span>
                  </li>
                ))}
              </ul>
              <div className="flex items-center space-x-3 text-sm text-slate-500 border-t border-white/5 pt-6">
                <Lock className="w-4 h-4" />
                <span>Your data is strictly private and never shared with GPs.</span>
              </div>
            </div>
            
            <SignUpForm />
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-[#05080F] border-t border-white/5 pt-16 pb-8">
        <div className="container mx-auto px-6">
          <div className="grid md:grid-cols-4 gap-12 mb-12">
            <div className="col-span-1 md:col-span-2">
              <div className="flex flex-col mb-4">
                 <span className="text-xs text-slate-500 uppercase tracking-widest mb-1">Powered by</span>
                 <span className="text-2xl font-bold text-white tracking-tight">vyzer.co</span>
              </div>
              <p className="text-slate-500 max-w-sm text-sm leading-relaxed">
                The intelligent vetting engine for Limited Partners. We bring clarity, speed, and standardization to private market investing.
              </p>
            </div>
            <div>
              <h4 className="text-white font-bold mb-4 text-sm uppercase tracking-wider">Platform</h4>
              <ul className="space-y-2 text-slate-500 text-sm">
                <li><a href="#" className="hover:text-blue-400 transition-colors">Features</a></li>
                <li><a href="#" className="hover:text-blue-400 transition-colors">Pricing</a></li>
                <li><a href="#" className="hover:text-blue-400 transition-colors">Security</a></li>
                <li><a href="#" className="hover:text-blue-400 transition-colors">Benchmarks</a></li>
              </ul>
            </div>
            <div>
              <h4 className="text-white font-bold mb-4 text-sm uppercase tracking-wider">Company</h4>
              <ul className="space-y-2 text-slate-500 text-sm">
                <li><a href="#" className="hover:text-blue-400 transition-colors">About Us</a></li>
                <li><a href="#" className="hover:text-blue-400 transition-colors">Contact</a></li>
                <li><a href="#" className="hover:text-blue-400 transition-colors">Privacy Policy</a></li>
                <li><a href="#" className="hover:text-blue-400 transition-colors">Terms of Service</a></li>
              </ul>
            </div>
          </div>
          <div className="border-t border-white/5 pt-8 text-center text-slate-600 text-xs">
            &copy; {new Date().getFullYear()} Vyzer Financial Technologies. All rights reserved.
          </div>
        </div>
      </footer>
    </div>
  );
}